# TODO — kleinanzeigen-ai
# Last updated: 2026-06-29
# Items without a checkbox are section headers.

=============================================================================
1. SECRETS & ENV VARS  (do these before anything else runs)
=============================================================================
- [ ] Set ACR_LOGIN_SERVER  e.g. myregistry.azurecr.io
- [ ] Set ACR_USERNAME      ACR admin username
- [ ] Set ACR_PASSWORD      ACR admin password
- [ ] Set GOOGLE_CLIENT_ID  from Google Cloud Console → OAuth 2.0 credentials
- [ ] Set GOOGLE_CLIENT_SECRET  same place
- [ ] Set DATABASE_URL  postgresql://user:pass@host:5432/kleinanzeigen_ai
- [ ] Set REDIS_URL  redis://... or rediss://... connection string
- [ ] Set SECRET_KEY  generate with: openssl rand -hex 32
- [ ] Set ENVIRONMENT  dev | staging | prod per deployment
- [ ] Set OCTOPUS_SERVER_URL  e.g. https://yourinstance.octopus.app
- [ ] Set OCTOPUS_API_KEY  from Octopus → Profile → API Keys

=============================================================================
2. SECURITY — URGENT
=============================================================================
- [ ] Set cookie secure=True in app/api/routers/auth.py when ENVIRONMENT != dev
- [ ] Restrict CORS allow_origins from ["*"] to your frontend domain(s) in production

=============================================================================
3. AUTHENTICATION
=============================================================================
- [ ] Look up user from DB in get_current_user() — currently returns a plain dict
      without verifying the user exists; add db.query(User).filter(User.id == user_id).first()
- [ ] Raise HTTP 401 if user is not found or is inactive
- [ ] Register Google OAuth redirect URI in Google Cloud Console:
        https://yourdomain.com/auth/google/callback
- [ ] Test the full Google OAuth flow end-to-end in dev

=============================================================================
4. API — MISSING ENDPOINTS
=============================================================================
- [ ] Add 422 response with clear message if required scrape parameters are missing
- [ ] Add /dashboard auth guard — currently anyone can hit /dashboard without a token

=============================================================================
5. DATABASE
=============================================================================
- [ ] Provision Azure Database for PostgreSQL (Flexible Server) in Azure Portal
- [ ] Run migrations against prod: alembic upgrade head
- [ ] Add unique constraint on ScrapeResult.url per task_id (avoid duplicate rows)
- [ ] Add asyncpg if/when switching to async SQLAlchemy engine

=============================================================================
6. REQUIREMENTS & DOCKERFILES
=============================================================================
- [ ] Consolidate pyproject.toml vs requirements.txt — pick one and use it everywhere
        Currently: api+worker+beat Dockerfiles all use requirements.txt; pyproject.toml
        is present but only partially complete. Recommended: use requirements.txt only.
- [ ] Create requirements-dev.txt for dev/test deps (pytest, ruff, mypy, httpx)
- [ ] Pin base images to a specific digest for reproducibility:
        e.g. python:3.11-slim@sha256:<digest>
- [ ] Consider multi-stage Docker builds to reduce final image size

=============================================================================
7. CI/CD (.github/workflows/build-and-push.yml)
=============================================================================
- [ ] Add GitHub secrets: OCTOPUS_SERVER_URL, OCTOPUS_API_KEY (see TODO.md Step 3)
- [ ] Configure Octopus project "kleinanzeigen-ai" with environments Dev/Staging/Prod
- [ ] Pin action versions to SHA for supply-chain security
        e.g. actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af68  # v4.2.2

=============================================================================
8. OCTOPUS DEPLOY — PROJECT SETUP
=============================================================================
- [ ] Create project "kleinanzeigen-ai" in Octopus Deploy
- [ ] Create environments: Dev, Staging, Prod
- [ ] Configure deployment process to run api + worker + beat containers from ACR
- [ ] Set project variables per environment:
        DATABASE_URL, REDIS_URL, SECRET_KEY, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
- [ ] Configure lifecycle: Dev → Staging → Prod (manual promotion after Dev)
- [ ] Add a rollback step/runbook in Octopus for emergency revert

=============================================================================
9. CELERY BEAT SCHEDULE
=============================================================================
- [ ] Review and adjust scrape schedule (currently every 30 min, hardcoded category)
- [ ] Add schedules for additional categories as needed
- [ ] Consider storing schedules in PostgreSQL via django-celery-beat or similar
        for runtime editing without a redeploy

=============================================================================
10. SCRAPING — REMAINING WORK
=============================================================================
- [ ] Add pagination support: scrape multiple pages per job (page param in URL)
- [ ] Respect rate limits: add configurable delay between requests
- [ ] Add unit tests for url_builder covering all parameter combinations
- [ ] Validate that HTML selectors still match kleinanzeigen.de
        (site structure changes frequently — selectors may need updating)

=============================================================================
11. PRODUCTION READINESS
=============================================================================
- [ ] Add rate limiting middleware before exposing the API publicly
- [ ] Add startup/shutdown lifespan handlers for DB connection pool warm-up
- [ ] Update docs/architecture.md with final infra diagram and data flow
- [ ] Add CONTRIBUTING.md with branching strategy and PR guidelines
- [ ] Add unit tests (pytest) for API routes and Celery tasks

25. BUG FIXES — CANCEL RACE / PRICE SELECTOR / KEYWORD URL (2026-07-02)
- [x] Fix cancel race: added cancelled guard in scrape_kleinanzeigen() before
      setting status="running" — queued runs that start after a cancel now bail
      immediately without overwriting the "cancelled" status or re-scheduling
- [x] Fix price selector: updated to aditem-main--middle--price-shipping--price
      (kleinanzeigen renamed the element); old class kept as fallback for cached HTML
- [x] Fix keyword-only URL 400: url_builder now produces /s-{keyword}/k0 for
      keyword-only searches instead of the rejected /s-anzeigen/{keyword}/k0 shape
      — category+keyword path (/s-{category}/{keyword}/k0) unchanged and still works
