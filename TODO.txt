# TODO — kleinanzeigen-ai
# Last updated: 2026-07-06
# Items without a checkbox are section headers.
# The app is live in production (self-managed VPS, Docker Compose + Caddy,
# auto-deployed on push to main — see docs/vps-deployment.md). Sections 1, 7,
# and 8 below predate that decision and describe an abandoned GCP/Cloud Run
# path; kept for history, not as live instructions.

=============================================================================
1. SECRETS & ENV VARS  — SUPERSEDED (see .env.example + docs/vps-deployment.md)
=============================================================================
- [~] GCP_WORKLOAD_IDENTITY_PROVIDER / GCP_SERVICE_ACCOUNT / GCP_PROJECT_ID /
      GCP_REGION — obsolete, no GCP deploy path exists (infra/gcp-setup.sh
      referenced here was never added to this repo)
- [x] GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET — set, Google login is live
- [x] DATABASE_URL / REDIS_URL / SECRET_KEY / ENVIRONMENT — set for prod
- [ ] The real first-deploy checklist now lives in .env.example — keep that
      file current instead of duplicating variable names here

=============================================================================
2. SECURITY — URGENT
=============================================================================
- [x] Cookie secure=True in app/api/routers/auth.py when ENVIRONMENT != dev
- [x] Restrict CORS allow_origins from ["*"] to dev only (app/api/main.py)

=============================================================================
3. AUTHENTICATION
=============================================================================
- [x] get_current_user() looks the user up in the DB and checks is_active
      (app/api/dependencies.py)
- [x] Raises HTTP 401 if the user is not found or inactive
- [x] Google OAuth redirect URI registered; Google sign-in is live in prod
- [x] Bootstrap admin login (username/password fallback on /auth/login) is
      now gated behind BOOTSTRAP_ADMIN_ENABLED, and app/api/config.py
      refuses to start outside dev unless APP_PASSWORD is non-default and
      ≥12 characters (2026-07-06)

=============================================================================
4. API — MISSING ENDPOINTS
=============================================================================
- [ ] Add a proper 422 with a clear message if required scrape parameters
      are missing (currently: soft validation + flash-redirect to
      /dashboard, which is arguably fine as UX but isn't a JSON API contract)
- [x] /dashboard auth guard — redirects to / if the session is invalid

=============================================================================
5. DATABASE
=============================================================================
- [~] Provision Cloud SQL — superseded, Postgres runs as a container via
      docker-compose.prod.yml on the VPS, not Cloud SQL
- [x] Migrations run against prod automatically on every deploy
      (`alembic upgrade head` in .github/workflows/ci-cd.yml)
- [ ] Add unique constraint on ScrapeResult.url per task_id (avoid duplicate
      rows) — still open, dedup is currently done in application code
      (app/worker/tasks.py) rather than enforced by the schema
- [~] asyncpg — rejected; CLAUDE.md now documents "Synchronous SQLAlchemy
      only" as a firm constraint, not a TODO

=============================================================================
6. REQUIREMENTS & DOCKERFILES
=============================================================================
- [ ] Consolidate pyproject.toml vs requirements.txt — pick one and use it
      everywhere. Currently: api+worker+beat Dockerfiles all use
      requirements.txt; pyproject.toml is present but only partially
      complete. Recommended: use requirements.txt only.
- [ ] Create requirements-dev.txt for dev/test deps (pytest, ruff, mypy, httpx)
- [ ] Pin base images to a specific digest for reproducibility:
        e.g. python:3.11-slim@sha256:<digest>
- [ ] Consider multi-stage Docker builds to reduce final image size

=============================================================================
7. CI/CD (.github/workflows/build-and-push.yml)  — SUPERSEDED
=============================================================================
- [~] This whole section described a GCP Artifact Registry / Workload
      Identity Federation / Octopus Deploy pipeline that was never built.
      The actual pipeline is .github/workflows/ci-cd.yml: lint + test on
      every push/PR, then SSH deploy to the VPS on push to main. See
      README.md's CI/CD section.
- [ ] Pin GitHub Action versions to SHA for supply-chain security
      e.g. actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af68  # v4.2.2
      (still a valid open item against the real workflow file)

=============================================================================
8. CLOUD RUN DEPLOY — ROLLBACK  — SUPERSEDED
=============================================================================
- [~] No Cloud Run deployment exists. Rollback on the VPS today means:
      `git revert` the bad commit and push to main (redeploys via CI/CD),
      or SSH in and `git checkout <previous-sha> && docker compose -f
      docker-compose.prod.yml up -d --build`.
- [ ] Write that rollback runbook down properly in docs/vps-deployment.md
      instead of leaving it here

=============================================================================
9. CELERY BEAT SCHEDULE
=============================================================================
- [x] Beat's schedule now also runs the data-retention purge daily
      (archival.cleanup_old_results at 03:00, archival.cleanup_old_token_usage
      at 03:15 Europe/Berlin) — both tasks existed in app/worker/archival_task.py
      but were never actually scheduled until 2026-07-06
- [ ] Review and adjust the admin-search dispatch interval (currently
      dispatched every 60s, each AdminSearch's own interval_minutes controls
      its effective cadence, default 30 min, hardcoded category)
- [ ] Add schedules for additional categories as needed
- [ ] Consider storing schedules in PostgreSQL via django-celery-beat or
      similar for runtime editing without a redeploy

=============================================================================
10. SCRAPING — REMAINING WORK
=============================================================================
- [ ] Add pagination support: scrape multiple pages per job (page param in URL)
- [ ] Respect rate limits: add configurable delay between requests
- [ ] Add unit tests for url_builder covering all parameter combinations
- [ ] Validate that HTML selectors still match kleinanzeigen.de
        (site structure changes frequently — selectors may need updating)

=============================================================================
11. PRODUCTION READINESS
=============================================================================
- [~] Rate limiting exists per-route on auth endpoints (slowapi — login,
      register, verify) but there's no general API-wide rate limiting
      middleware; still worth adding before wider public exposure
- [ ] Add startup/shutdown lifespan handlers for DB connection pool warm-up
- [ ] Update docs/architecture.md with final infra diagram and data flow
- [ ] Add CONTRIBUTING.md with branching strategy and PR guidelines
- [ ] Add unit tests (pytest) for API routes and Celery tasks — still true,
      there are no automated tests in this repo yet

25. BUG FIXES — CANCEL RACE / PRICE SELECTOR / KEYWORD URL (2026-07-02)
- [x] Fix cancel race: added cancelled guard in scrape_kleinanzeigen() before
      setting status="running" — queued runs that start after a cancel now bail
      immediately without overwriting the "cancelled" status or re-scheduling
- [x] Fix price selector: updated to aditem-main--middle--price-shipping--price
      (kleinanzeigen renamed the element); old class kept as fallback for cached HTML
- [x] Fix keyword-only URL 400: url_builder now produces /s-{keyword}/k0 for
      keyword-only searches instead of the rejected /s-anzeigen/{keyword}/k0 shape
      — category+keyword path (/s-{category}/{keyword}/k0) unchanged and still works

26. RESULTS PAGE — NEW BADGE / SUBCATEGORY TABS / PAGINATION (2026-07-02)
- [x] NEW badge: keyed to last-visit cookie rv_{task_id} (Unix timestamp, 30-day, httponly)
      results with created_at > last_visit are flagged is_new; first visit shows no badges
      cookie updated to now() on every response so next visit compares correctly
- [x] Subcategory tabs: All (N) | New (N) + up to 4 most-common unique location tabs
      tab state preserved in URL (?tab=all|new|<location>&page=N)
- [x] Pagination: 10 results per page, prev/next + numbered pages with ellipsis
      page info footer shows "X–Y of Z" for current slice
- [ ] Consider server-side tab counts for location tabs (currently client counts)
- [ ] Add keyboard navigation for tab bar (arrow keys)

27. SEARCH FORM UI — COMPACT LAYOUT / RADIUS AFTER PLZ (2026-07-02)
- [x] Removed the 5-second frequency option from the search form (was dev-only,
      never enforced as a plan floor) — Frequency now offers Instant/5min/30min/60min
- [x] Reworked New Recurring Search form into a responsive 2-column grid
      (Keywords+Category, Price min/max, Angebotstyp+Anbieter, Zustand+Versand
      all sit side by side) to cut vertical scroll on the dashboard
- [x] Radius field is now hidden until a location is chosen from the PLZ/city
      autocomplete; relabeled "Live radius" and rendered directly below the
      location field once a suggestion is selected. Clearing/editing the
      location field hides the radius control again and resets its value
- [ ] Consider persisting last-used search filters (keywords, category, radius)
      per user so the form pre-fills on next visit

28. SEARCH CREATION WIZARD — TWO-STEP FLOW (2026-07-02)
- [x] Split "New Recurring Search" into two separate steps: Step 1 mirrors a
      plain kleinanzeigen.de search (keywords, category, location, radius,
      price range, ad type, poster type, condition, shipping); Step 2 is
      purely for choosing the check frequency
- [x] Search creation only completes (form submits) after Step 2 — the
      previous single-page form behavior/backend contract is unchanged,
      this is purely a client-side UX restructure (same POST /scrapes/ payload)
- [x] Added a step indicator (1 Search -> 2 Frequency) and a short summary
      recap of the Step 1 choices shown at the top of Step 2
- [x] Step 1 requires at least a keyword or a location before advancing
      (soft client-side check, mirrors how a real kleinanzeigen.de search works)
- [x] Added a "Smart Alerts — coming soon" promo box on Step 2 as the
      designated slot for future AI/smart-feature upsells (deal scoring,
      price-drop predictions, etc.) — no functionality yet, teaser only
- [ ] Wire up real AI/smart features in the Step 2 promo slot once designed
      (deal scoring, price predictions, smart summaries)
- [ ] Consider saving step-1 draft to localStorage so a refresh doesn't lose
      the user's in-progress search

29. DASHBOARD GERMAN TRANSLATION + FORM TWEAKS (2026-07-02)
- [x] Translated the entire dashboard page (app/api/templates/dashboard.html)
      to German: <html lang="de">, all labels/buttons/table headers/empty
      states/admin panel text and JS-driven strings (notification banner,
      live task table renderer, wizard summary/validation text)
- [x] Merged the search wizard's Min Price / Max Price fields into a single
      "Preis (€)" row with two inputs side by side instead of two separate
      grid cells, in app/api/templates/dashboard.html + style.css
      (.price-range-field / .price-range-inputs)
- [x] Renamed wizard Step 2 from "Frequency" to "Smart Enhancements" in the
      step indicator and the Step 1 "Continue" button label (the panel still
      contains the frequency radios plus the "Smart Alerts" promo box —
      "Smart Enhancements" is now the umbrella name for that step)
- [ ] The admin panel ("Add Background Search" etc.) is now in German too —
      confirm with any non-German-speaking admins before this ships, or add
      a language toggle if the app needs to support both audiences
- [ ] Consider translating email templates / push notification copy to
      German as well for full consistency now that the dashboard is German

30. COMMERCIAL-READINESS AUDIT FIXES (2026-07-06)
- [x] Onboarding tutorial's "done" button posted to a /settings/tutorial-
      complete route that never existed — added it, has_completed_tutorial
      now actually persists
- [x] Email notifications were fully dead: the sender called a nonexistent
      Resend client API and read its key independently of app config. Now
      uses the same Resend HTTP call as verification emails, fires at the
      same point as push, and the settings copy matches the real behavior
      (per-result email, not the "weekly summary" it used to claim)
- [x] Scheduled the existing 14-day result / 90-day token-usage retention
      purge tasks in Celery Beat — implemented for a while, never invoked
- [x] Gated the hardcoded bootstrap-admin login behind BOOTSTRAP_ADMIN_ENABLED
      and a ≥12-character password requirement outside dev
- [x] Added account deletion (POST /settings/delete-account) and a JSON data
      export (GET /settings/export) — GDPR Art. 17 / 20
- [x] Made /billing viewable without a session and added a public landing
      page at / (previously pricing was hidden behind the login wall and
      "/" only ever showed the bare login form or bounced to /dashboard)
- [x] Restored the admin dashboard UI (background searches, proxy
      management) — it had been trimmed down to a single test button even
      though the backend and full markup already existed in an unrouted
      dashboard-backup.html. Deleted that file and another dead one,
      dashboard-phase4.html.
- [x] Added per-device push subscription management on /settings — a lost
      or old device's subscription can now be revoked from the server side,
      not just from the browser that created it
- [ ] Remaining from that audit: /locations/suggest has no auth or
      route-level rate limit (open, unauthenticated proxy to kleinanzeigen.de's
      own suggestion API)
